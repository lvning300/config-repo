server:
  port: 8885
spring:
  application:
    name: hello-service
  profiles:
    active: ${profile}
  cloud:
    config:
      uri: ${config.uri}

book:
  name: SpringCloudInAction
  author: LvNing
  version: 8886-sit